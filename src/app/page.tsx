"use client";

import { ImagePlay, Smile, Send } from "lucide-react";
import { useTheme } from "next-themes";

export default function Chat() {
  const { resolvedTheme } = useTheme();

  return <div className="h-full flex flex-col relative"></div>;
}
